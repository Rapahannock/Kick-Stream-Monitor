
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Kick Streamers Monitor Pro v3.3</title>
  <style>
    :root {
      --bg: #111;
      --text: #eee;
      --card: #1a1a1a;
      --border: #444;
      --accent: #4af;
      --toast-bg: #333;
      --toast-text: #fff;
    }
    [data-theme="light"] {
      --bg: #f4f4f4;
      --text: #222;
      --card: #fff;
      --border: #ccc;
      --accent: #06c;
      --toast-bg: #ddd;
      --toast-text: #000;
    }
    body {
      font-family: Arial, sans-serif;
      background-color: var(--bg);
      color: var(--text);
      padding: 20px;
      transition: background 0.3s, color 0.3s;
    }
    #streamers {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 12px;
    }
    .streamer {
      padding: 12px;
      border: 1px solid var(--border);
      border-radius: 6px;
      background-color: var(--card);
      position: relative;
    }
    .offline {
      opacity: 0.5;
    }
    .live {
      outline: 2px solid var(--accent);